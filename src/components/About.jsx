import React from "react";

const About = () => {
    return (
        <section className="about">
            <h2>About Me</h2>
            <p>Frontend developer. I work with React, JavaScript, HTML, CSS.</p>
        </section>
    );
}

export default About;
